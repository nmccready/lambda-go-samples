# lambda-go-samples

see: `make help`

## dependencies

- `npm install -g foreman`
- `brew install jq`

## deployment example

`nf run make deploy`

## un-deploy (destroy stack)

`nf run make undeploy`
